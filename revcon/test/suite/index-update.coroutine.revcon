@coroutine

coroutine main(list int xs) {
  local int x

  push x xs
  push x xs
  push x xs
  push x xs

  xs[0] += 1
  xs[1] += 3
  xs[2] += 5
  xs[3] += 8

  push x xs
  xs[1] += 10

  delocal int x
}
